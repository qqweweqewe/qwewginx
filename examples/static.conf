worker_processes 1;

events {
    worker_connections 1024;
}

http {
    server {
        listen 127.0.0.1:9090;
        location / {
            root examples/static;
            index index.html;
        }
    }
}
