worker_processes 1;

events {
    worker_connections 1024;
}

http {
    access_log examples/access.log;

    server {
        listen 127.0.0.1:9090;
        location / {
            return 200 "access-log demo\n";
        }
    }
}
