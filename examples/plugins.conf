worker_processes 1;

plugins {
    hello 0.1.0 from local {
        listen 127.0.0.1:9098;
        path /hello;
    }
}

events {
    worker_connections 1024;
}

http {
    server {
        listen 127.0.0.1:9090;
        location / {
            return 200 "plugins example\n";
        }
    }
}
